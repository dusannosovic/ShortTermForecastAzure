@model long
@{
    ViewBag.Title = "UploadFile";
}
<h2>UploadFile</h2>
@using (Html.BeginForm("UploadFile", "Home", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <div>
        @Html.TextBox("file", "", new { type = "file" }) <br />
        <input type="submit" value="Ubaci podatke" />
        @ViewBag.Message
    </div>
}

    @using (Html.BeginForm("Training", "Home", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        <div>
            <input type="submit" value="Training" />
            @ViewBag.Message
        </div>
    }

<h1>Broj redova je: @Model</h1>